# 📝 更新日志

所有格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/)。

---

## [Web重构 v26.14.4] - 2026-07-30

### 新增

- **设备同源版本检查** — 关于页优先通过 UFI-TOOLS `/api/proxy/--` 读取 GitHub 仓库、文件列表和最新 WebOS 源码
- **最新文件发现** — 自动扫描 `Web重构_v*.js`，不再只请求当前文件名，版本号、分支和 Stars 同步更新
- **本地服务同源转发** — GitHub 直连和浏览器私网请求被拦截时，使用设备后端代理访问本地更新服务

## [运营商信息 v1.3.0] - 2026-07-30

### 新增

- **广电官方短信自动读取** — 打开后自动读取并持续轮询 `10099` 回执，支持 Base64 和 Unicode 解码，无需点击查询
- **API 自动发现** — 自动尝试已保存地址、Mac 局域网服务和本机服务，自动选择直连或 UFI-TOOLS 同源转发
- **被动自动刷新** — 官方短信每分钟读取，API 结果每 5 分钟同步；手动按钮仅作为立即刷新补充
- **载入即查询** — 插件启动后立即识别 SIM 并读取官方结果，关闭运营商面板后仍保持后台轮询

### 优化

- **结果优先级** — API 未授权或不可达时，不再遮盖已成功解析的 `10099` 官方短信结果
- **配置界面简化** — 移除手工 API 地址输入，仅在首次授权或登录态失效时显示可选的 API 授权入口

## [10099 Tracker v1.3] - 2026-07-30

### 新增

- **后台定时查询** — 服务启动后默认每 15 分钟刷新官方流量，可用 `TRAFFIC_REFRESH_SECONDS` 调整
- **持久化结果缓存** — 最近成功的非敏感流量结果写入 `data/traffic_cache.json`，服务重启后仍可立即读取
- **自动化状态** — `/health` 和 `/traffic` 返回刷新周期、最近尝试、最近成功、缓存与授权失效状态

### 优化

- **过期回退** — 官方登录态失效或上游暂时不可达时，返回最近成功结果并标记 `stale` 和 `authorization_required`

## [Web重构 v26.14.3] - 2026-07-30

### 优化

- **手动强制刷新** — 关于页点击“立即检查”时将强制本地代理刷新 GitHub 数据，不再继续使用 10 分钟服务端缓存
- **过期缓存兜底** — GitHub 临时不可达时，`10099-Tracker` 返回最后一次成功的版本结果，并显式标记为过期缓存
- **状态提示** — WebOS 可识别代理缓存状态，避免把离线回退结果误报为刚刚更新

## [Web重构 v26.14.2] - 2026-07-30

### 新增

- **本地更新代理** — `10099-Tracker` 新增公开只读的 `/project-version`，代查 GitHub 默认分支中的最新 WebOS 源码版本和 Stars
- **自动回退** — 关于页直连 GitHub 受浏览器策略拦截时，优先复用运营商插件已配置的本地 API 主机，再尝试 `127.0.0.1:8000`

### 优化

- **代理缓存** — 本地服务缓存公开仓库结果 10 分钟，减少 GitHub 匿名 API 限额消耗
- **私有网络兼容** — 更新代理增加 CORS 与 Private Network Access 响应头，支持 F50 页面从局域网或本机调用

## [Web重构 v26.14.1] - 2026-07-30

### 修复

- **保存前登录检查** — 未登录设备时直接给出可操作提示，不再将空响应统称为“保存接口返回失败”
- **保存响应兼容** — 支持空 body、布尔值、`success` / `ok`、数字状态和常见 JSON 字段
- **写后回读校验** — 写入 custom head 后最多回读三次并比对完整源码，只有设备端已持久化才会刷新页面

### 优化

- **接口诊断** — 区分 HTTP 错误、空内容、返回格式异常和回读不一致，便于定位设备登录或后端故障

## [Web重构 v26.14.0] - 2026-07-29

### 新增

- **关于页升级** — 自动读取 GitHub 默认分支中的 WebOS 源码版本、更新分支、检查时间和仓库 Stars
- **版本检查缓存** — 结果缓存 6 小时，网络失败时保留上一次成功结果，不阻塞设置中心打开
- **仓库支持入口** — 增加“给仓库点 Star”、支持者列表、问题反馈和源码入口；不会代替用户操作 GitHub 账号
- **首页更新提示** — 仅在远程版本高于本地版本时显示一条可关闭的低干扰提示

### 优化

- **版本比较** — 只比较数字版本段，避免把电话插件的 `v1.x` Tag 误判为 WebOS 控制台版本
- **关于页布局** — 重排版本、支持项目、项目资料和适配策略信息，小屏改为单列并避免长文本挤压

## [10099 Tracker 本地服务] - 2026-07-29

### 新增

- **官方接口代理** — 将 `10099-Tracker` 的 `qryUserRes` 请求和流量资源汇总逻辑集成到仓库，与运营商信息插件的 `/traffic?details=1` 协议直接兼容
- **双部署方式** — 同时提供 Docker Compose 与 macOS/Linux 原生 Python 启动脚本；未安装 Docker 时也能运行
- **抓包配置页** — 支持粘贴完整 cURL，自动提取 `Session`、`Access`、`User-Agent` 和加密 `data`

### 安全

- **配置脱敏** — 管理 API 不再把完整登录态返回浏览器，敏感配置仅写入本机 `data/config.json`
- **管理令牌** — 配置读取与 cURL 更新接口需要管理令牌，公开的插件接口只返回流量查询结果

## [Web重构 v26.13.0] - 2026-07-29

版本标识：`26.13.0-settings-center`

### ✨ 优化

- **设置中心重构** — 桌面端改为固定侧栏与单一内容滚动区，页头和底部操作保持稳定
- **功能中心归并** — 五个原生功能分类收进一个一级入口，在页面内使用紧凑二级导航切换
- **移动端导航** — 设置分类改为下拉选择，不再同时展示纵向或横向滚动标签
- **界面密度统一** — 导航、卡片、按钮、输入框和状态区域使用一致的间距与圆角规则
- **插件管理适配** — 保留列表与源码编辑器的独立滚动，修复手机端标题、读取状态和说明文字重叠
- **消息转发适配** — 修复当前通道选择框在窄列中被截断，保留登录、读取、保存和未保存状态保护
- **切页作用域** — 设置标签和面板切换限制在设置对话框内部，避免影响其他插件的同名组件

### 🧪 验证

- **真实设备检查** — 在 F50 / UFI-TOOLS v4.0.9 页面检查桌面和 390px 手机宽度，覆盖导航、功能中心、外观、消息转发、插件管理与兼容性页面

## [运营商信息 v1.2.0] - 2026-07-29

### 🎉 新增

- **广电本地 API 查询** — 支持连接自托管代理，读取中国广电官方小程序接口返回的总流量、已用流量、剩余流量、更新时间和资源明细
- **10099 一键拨号** — 调用“电话与短信”插件直接拨打中国广电客服
- **代理错误分级** — 区分登录态过期、代理配置错误、官方上游错误、跨域/网络错误和请求超时

### 🐛 修复

- **停用无效短信流程** — 实卡验证 `10099 → 菜单 → 1/2` 均返回“指令有误或不存在”，不再将“收到菜单”标记为可继续查询
- **结果优先级** — 广电本地 API 的当前官方结果不再被历史短信菜单或拒绝回执覆盖
- **渠道命名** — 中国广电入口改为“官网”，不再暗示网页一定提供已登录的余额查询

### 🔒 安全

- **凭据隔离** — 插件仅保存本地代理 URL；`Access`、`Session` 和加密请求数据留在自托管代理端

## [运营商信息 v1.1.0] - 2026-07-29

### 🎉 新增

- **广电服务菜单回执** — 可识别向 `10099` 发送 `10099` 后返回的官方查询/办理菜单（后续验证表明序号交互不可用，已在 v1.2.0 停用入口）
- **官方营业厅入口** — 为移动、联通、电信与广电增加官方网页查询入口

### 🐛 修复

- **无效指令识别** — 将“指令有误或不存在”等回执标记为运营商拒绝，避免误报查询成功
- **最新回执优先** — 最新回复为拒绝提示或服务菜单时，不再回退展示较旧的套餐结果

### 🔒 安全

- **不内置 App 抓包凭据** — App 接口依赖动态登录态、签名与设备参数，插件继续采用官方短信和网页渠道

## [运营商信息 v1.0.1] - 2026-07-28

### 🐛 修复

- **中文短信解码** — 支持设备返回的 Base64 UTF-8 运营商回执，同时保留 UCS2 十六进制兼容
- **当前官方结果** — 按新到旧逐条解析官方回复，避免较旧短信混入或覆盖最近结果
- **广电流量回执** — 支持组合流量单位以及“流量包已用尽”状态，剩余流量明确显示为 `0 MB`
- **回执来源提示** — 官方查询结果显示最近回执时间，未识别或暂无回执时给出明确状态

---

## [Web重构 v26.12.0] - 2026-07-28

版本标识：`26.12.0-webos-network-diagnostics`

### 🎉 新增

- **网络诊断面板** — 集中展示连接、运营商、SIM、RSRP/RSRQ/SINR、吞吐、累计流量、网络地址与系统资源
- **原生工具联动** — 根据当前固件动态汇总内外网测速、流量历史、IP 查询和网络诊断入口
- **脱敏诊断报告** — 一键复制诊断信息，隐藏 IPv4/IPv6 尾段且不包含手机号、IMEI、IMSI 与 ICCID

### ✨ 优化

- **CPU 核心明细** — 默认直接显示在单行横向区域，无需点击展开
- **响应式诊断布局** — 桌面双列、手机单列，固定操作区并保持单一内容滚动
- **热重载清理** — 重复加载插件时自动移除旧诊断弹层，避免重复 DOM 与事件残留
- **安静登录态** — 未登录时暂停首页、顶栏和原生转发轮询，改为明确的“登录后读取”状态
- **移动端设置** — 设置标签改为紧凑的单行横向滚动，避免纵向菜单占满首屏
- **消息转发布局** — 标题与操作区分行排列，修复窄内容区标题竖排和横向溢出
- **消息转发状态** — 增加未读取、未保存、保存中与失败状态，未登录时禁用写入控件，重新读取前保护未保存内容
- **转发校验与隐私** — 关闭原生转发时可直接保存，增加 SMTP/Webhook 校验和密码显示开关，电话测试改为不发送的模板预览
- **导航模块移动** — 每个页面模块和插件面板都可直接选择目标分组，保留桌面拖拽，增加分组数量和移动反馈
- **原生界面让位** — WebOS 启用期间隐藏原页脚与右侧刷新浮条，卸载后自动恢复

---

## [运营商信息 v1.0.0] - 2026-07-28

### 🎉 新增

- **运营商自动识别** — 结合运营商名称、IMSI/PLMN 和 ICCID 识别移动、联通、电信与广电
- **SIM 与网络信息** — 集中展示 SIM 状态、手机号、网络制式、信号、漫游及设备标识
- **官方短信查询** — 支持向 `10086`、`10010`、`10001` 发送余额/流量查询指令
- **回执自动解析** — 提取话费、剩余/总/已用流量、语音、短信、套餐名称与有效期
- **回复自动追踪** — 查询后在 90 秒内轮询运营商服务短信，并避免将已发送记录误判为回复
- **WebOS 集成** — 实现 `window.KanoOperatorInfo` / `window.KanoCarrierInfo` 插件契约
- **隐私保护** — 默认脱敏显示手机号、IMSI、ICCID 和 IMEI，不引入第三方查询服务
- **入口去重** — 检测到 WebOS 首页集成卡片后不再创建独立悬浮入口

---

## [Web重构 v26.3.3] - 2026-05-26

版本标识：`26.3.3-header-generic-mood-icon`

### 🎨 新增

- **信号心情图标 (Mood Icon)** — 顶栏品牌标识实时反映信号状态，6 级渐变色自动切换
  | 信号等级 | Emoji | 样式 | 含义 |
  |----------|-------|------|------|
  | ≥ 4 / 5 | 😊 | 绿色渐变 + 光晕 | 信号良好 |
  | 3 / 5 | 🙂 | 蓝色渐变 | 信号正常 |
  | 2 / 5 | 😟 | 黄色渐变 | 信号偏弱 |
  | 1 / 5 | 😞 | 橙色渐变 | 信号很弱 |
  | 0 / 5 | 😵 | 红色渐变 | 无信号 |
  | 未连接 | 😴 | 红色渐变 | 离线 |
- **CSS 纯绘登录图标** — 登录按钮头像改为 CSS 伪元素绘制，不再依赖 emoji
- **网络类型徽章 (nettype-badge)** — pill 样式标签，5G 绿 / 4G 蓝 / 3G 黄颜色区分
- **WiFi 状态胶囊** — 独立按钮显示 WiFi 开关状态 + 在线设备数
- **登录状态指示灯** — 登录按钮右下角绿/红圆点实时显示在线状态
- **最终打磨样式层** — `kn-header-final-polish-style` 精细调整所有顶栏组件间距和尺寸

### 🔧 改进

- `功能列表` 模块名简化（去掉 `/ 快捷操作` 后缀）
- `拓展工具箱` 修正为 `扩展工具箱`
- 新增 `mapLegacyGroup()` 函数，自动迁移旧版 `functionsPage` 分组到 `tools`
- 功能列表不再作为顶层导航页，仅在设置中作为独立模块（v26.2.9）
- Header 布局重构为 `minmax(245px,300px) minmax(390px,auto) max-content` 三栏自适应

---

## [Web重构 v2.0] - 2026-05-22

### 🎨 新增

- **架构重构** — 从三页 Tab 路由升级为五大分组导航（状态 / 网络 / 系统 / 工具 / 扩展）
- **全新 UI** — 顶部三栏布局 + 5 分组胶囊导航 + 毛玻璃 sticky 顶栏
- **日/夜间模式** — 新增日间模式 + 跟随系统，完整的 Light/Dark 双主题
- **背景图系统** — 支持 Bing 每日风景 / 随机摄影 / CSS 渐变 / 自定义 URL
- **设置面板** — 四页 Tab 设置（导航分组 / 界面美化 / 首页背景 / 关于）
- **面板分组** — 插件面板可自由拖拽到任意分组，或设为隐藏
- **10+ 美化选项** — 圆角 / 阴影 / 胶囊按钮 / 玻璃拟态 / 渐变标题 / 悬停动效 / 滚动条 / 柔和分割线 / 文字增强 / 紧凑布局
- **主题调色** — 强调色 / 渐变色 / 顶栏模糊 / 透明度 / 背景遮罩等参数可调
- **配置管理** — 导出 / 导入配置，恢复默认，刷新不丢失
- **响应式适配** — 平板/手机端自适应布局

### 🔧 改进

- 安全布局 — 不移动第三方插件 div，仅原地显隐，避免破坏原插件结构

---

## [Web重构 v1.0] - 2026-04-26

### 🎨 新增

- **三页 Tab 导航**（状态总览 / 核心工具 / 扩展应用）
- **页面排版控制面板** — 拖拽式模块路由分配
- **智能元素抓取引擎** — 自动归位标题、登录按钮、工具箱按钮
- **CSS 路由** — 通过 `data-kn-page-tab` 属性控制分页显隐
- **毛玻璃顶栏** — sticky 定位，滚动时始终可见

---

## [UTools-Beautifier v1.0.1] - 2026-05-08

### ♻️ 变更

- 去除界面美化功能，保留纯插件收纳（Bartender 模式）

---

## [UTools-Beautifier v1.0.0] - 2026-04-26

### 🎨 新增

- **8 项视觉特效** — 圆角卡片 / 悬浮阴影 / 胶囊按钮 / 玻璃拟态 / 紧凑布局 / 渐变标题 / 动态悬停 / 极简滚条
- **插件收纳系统 (Bartender)** — 三区域拖拽分配（主页可见 / 收纳箱 / 彻底隐藏）
- **界面工坊** — Tab 标签页设置（美化 / 收纳 / 关于）
- **GitHub 自动检查更新**引擎
- **防重复挂载** — 支持旧 DOM 清理 + 热重载
- 移动端响应式适配（768px 断点）

---

## [电话与短信 v1.3.3] - 2026-05-22

### 🎨 新增

- **拨号盘** — 支持号码输入、拨号、挂断
- **短信会话模式** — 聊天式短信收发界面
- **通话记录** — 来电 / 去电 / 未接记录
- **搜索与过滤** — 通话记录和短信快速搜索
